function decideWave() {
    if (keyCode) {
        if (wave == 1) {
            waveMuscle = 10;
            wavestart = false
            wave++
        }
    }
}

function isWaveFinished() {
    //if all enemies dead
    //  if(muscle)
}
